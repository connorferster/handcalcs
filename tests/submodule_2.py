import math

def different_calc(s: float, t: float, u: float, v: float) -> float:
    """
    Returns a value for testing function recursion ability of the parser.
    """
    return math.pi * s * t / (u * v) **2
